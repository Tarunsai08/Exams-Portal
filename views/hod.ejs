<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>HOD</title>
    <meta name="author" content="Tarun Sai" />
    <meta name="description" content="" />

    <!-- Tailwind -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      @import url("https://fonts.googleapis.com/css?family=Karla:400,700&display=swap");
      .font-family-karla {
        font-family: karla;
      }
      .bg-sidebar {
        background: #ffd700; /* Gold color */
      }

      .cta-btn {
        color: #ffd700; /* Gold color */
      }

      .upgrade-btn {
        background: #ffd700; /* Gold color */
      }

      .upgrade-btn:hover {
        background: #ffcc00; /* Slightly lighter shade of gold */
      }

      .active-nav-link {
        background: #e3b600; /* Gold color */
      }

      .nav-item:hover {
        background: #e3b600; /* Slightly lighter shade of gold */
      }

      .account-link:hover {
        background: #ffd700; /* Gold color */
      }
    </style>
  </head>
  <body class="bg-gray-200 font-family-karla flex">
    <aside class="relative bg-sidebar h-screen w-64 hidden sm:block shadow-xl">
      <div class="p-6">
        <a
          href="/hod"
          class="text-black hover:text-white text-3xl font-semibold uppercase"
          >HOD</a
        >
      </div>
      <nav class="text-white text-base font-semibold pt-3">
        <a
          href="/hod"
          class="flex items-center active-nav-link text-white py-4 pl-6 nav-item"
        >
          <i class="fas fa-tachometer-alt mr-3"></i>
          Dashboard
        </a>
        <a
          href="/viewmarks"
          class="flex items-center text-black opacity-75 hover:text-white hover:opacity-100 py-4 pl-6 nav-item"
        >
          <i class="fas fa-sticky-note mr-3"></i>
          viewmarks
        </a>
        <a
          href="/selectqp"
          class="flex items-center text-black opacity-75 hover:text-white hover:opacity-100 py-4 pl-6 nav-item"
        >
          <i class="fas fa-align-left mr-3"></i>
          select Question paper
        </a>
      </nav>
    </aside>

    <div class="w-full flex flex-col h-screen overflow-y-hidden">
      <!-- Desktop Header -->
      <header class="w-full items-center bg-white py-2 px-6 hidden sm:flex">
        <div class="w-1/2">
          <h1 class="text-xl font-bold text-gray-700">Exam Portal</h1>
        </div>
        <div x-data="{ isOpen: false }" class="relative w-1/2 flex justify-end">
          <button
            @click="isOpen = !isOpen"
            class="realtive z-10 w-12 h-12 rounded-full overflow-hidden border-4 border-yellow-400 hover:border-yellow-300 focus:border-yellow-300 focus:outline-none"
          >
            <img
              src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQavfQMBYKkiT6iVeBYUBIQVdbdDaRMhM_EhRvBFubN9w&s"
            />
          </button>
          <button
            x-show="isOpen"
            @click="isOpen = false"
            class="h-full w-full fixed inset-0 cursor-default"
          ></button>
          <div
            x-show="isOpen"
            class="absolute w-32 bg-white rounded-lg shadow-lg py-2 mt-16"
          >
            <a href="/hod" class="block px-4 py-2 account-link">Home</a>
            <a href="/logout" class="block px-4 py-2 account-link">Sign Out</a>
          </div>
        </div>
      </header>

      <div id="content" class="w-full overflow-x-hidden border-t flex flex-col">
        <main class="w-full flex-grow p-6">
          <h1 class="text-3xl text-black pb-6">Dashboard</h1>

          <!-- Viewing Marks Section -->
          <section class="mb-8">
            <h2 class="text-2xl text-gray-800 mb-4">Viewing Marks</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
              <!-- Student Marks Card -->
              <div class="bg-white rounded-lg shadow-md p-6">
                <h3 class="text-xl font-semibold mb-4">Student Marks</h3>
                <p class="text-gray-600">View marks of individual students.</p>
                <a
                  href="/viewmarks"
                  class="mt-4 inline-block bg-yellow-400 hover:bg-yellow-500 text-white font-bold py-2 px-4 rounded"
                  >View Marks</a
                >
              </div>
            </div>
          </section>

          <!-- Selecting Question Papers Section -->
          <section>
            <h2 class="text-2xl text-gray-800 mb-4">
              Selecting Question Papers
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
              <!-- Select Question Paper Card -->
              <div class="bg-white rounded-lg shadow-md p-6">
                <h3 class="text-xl font-semibold mb-4">
                  Select Question Paper
                </h3>
                <p class="text-gray-600">
                  Select question papers for upcoming exams.
                </p>
                <a
                  href="/selectqp"
                  class="mt-4 inline-block bg-yellow-400 hover:bg-yellow-500 text-white font-bold py-2 px-4 rounded"
                  >Select</a
                >
              </div>
            </div>
          </section>
        </main>
      </div>
    </div>

    <!-- AlpineJS -->
    <script
      src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.x.x/dist/alpine.min.js"
      defer
    ></script>
    <!-- Font Awesome -->
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/js/all.min.js"
      integrity="sha256-KzZiKy0DWYsnwMF+X1DvQngQ2/FxF7MF3Ff72XcpuPs="
      crossorigin="anonymous"
    ></script>
  </body>
</html>
